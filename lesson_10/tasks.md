# Lesson 10 - In-Class Tasks: DOM and Events

Complete all tasks in class. Use `console.log()` where needed.

---

1. Select the element with id `para1` and print it.

---

2. Select all `p` elements and print how many were found.

---

3. Change the text of `#para1` to `This text was changed in JS`.

---

4. Change the text color of `#para1` to blue.

---

5. Add class `testClass` to `.para2`.

---

6. Remove class `testClass2` from the fourth paragraph.

---

7. Add a click event to `#btn1` that changes the button background to red.

---

8. In the same click event, also change button text color to white.

---

9. Add a click event to `#hideAndShowBtn` that toggles `hidden` on `#title`.

---

10. Add a click event to `.showBtn` that shows `.modal`.

---

11. Add a click event to `#close` that hides `.modal`.

---

12. Add a new paragraph with JavaScript and append it to the main content area.

---

13. Make the new paragraph text `Created from JavaScript`.

---

14. Add class `testClass` to the new paragraph.

---

15. Create a variable `isOpen` and track modal state (`true` or `false`) when opening and closing.

---

16. Print window width when `#btn1` is clicked.

---

17. If width is 768 or less, set button text to `Mobile Click`.

---

18. If width is above 768, set button text to `Desktop Click`.

---

19. Add a double-click event on `#title` to set its text to `Double Clicked`.

---

20. Add one keyboard event (`keydown`) and print the pressed key.
