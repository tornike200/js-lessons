# Lesson 10 - Homework: DOM and Events

Complete all 20 tasks at home.

---

1. Select `body` and change background color.

---

2. Select all buttons and print each button text.

---

3. Change the first heading text to `DOM Practice`.

---

4. Create a new button in JavaScript and append it to the page.

---

5. Add a click event to the new button that logs `New button clicked`.

---

6. Add class `hidden` to `#title`, then remove it after 2 seconds.

---

7. Create a list (`ul`) with 3 items using JavaScript.

---

8. Add a click event to each list item that changes its text color.

---

9. Build a simple counter with two buttons: `+` and `-`.

---

10. Show current counter value in a paragraph.

---

11. Open modal on button click.

---

12. Close modal when user clicks close icon.

---

13. Also close modal when user clicks outside the modal box.

---

14. Add keyboard support: close modal when `Escape` is pressed.

---

15. Print current page width on every resize event.

---

16. If width is mobile size (<= 768), add class `mobile-mode` to body.

---

17. If width is desktop size (> 768), remove class `mobile-mode` from body.

---

18. Add one form with input and button.

---

19. On submit, prevent page reload and print input value.

---

20. Clear the input after successful submit.

---

21. Design a simple article preview component with JavaScript and CSS.

[design link](https://www.figma.com/design/2atHm15GGUo258K5YNS8bJ/article-preview-component?node-id=0-1&p=f&t=MPyr5ihdHTQVxwnP-0)
