# Lesson 14 - Homework: API Calls in JavaScript

Complete all 20 tasks at home. These extend class tasks.

---

1. Start `json-server` and verify `/users` endpoint in browser.

---

2. Write `getUsers()` and print all users.

---

3. Print only user emails.

---

4. Write `getUserById(id)` and test with valid id.

---

5. Test `getUserById(id)` with invalid id and handle the result.

---

6. Create `createUser(user)` and add one new user.

---

7. Add three different users with three separate `POST` requests.

---

8. Write `updateUser(id, user)` and replace one user object.

---

9. Update another user using new email data.

---

10. Write `deleteUser(id)` and remove one user.

---

11. Remove another user and print confirmation.

---

12. Add `try/catch` to all CRUD functions.

---

13. Create helper `request(url, options)` and reuse it in CRUD functions.

---

14. Write function `getUserNames()` that fetches users and prints only names.

---

15. Write function `getUserByEmail(email)` that fetches users and finds the first matching email.

---

16. Create `resetUser(id)` that updates user with default values.

---

17. Create one function `runCrudTest()` that runs: create -> update -> delete.

---

18. Add console labels so each step output is easy to read.

---

19. Print final user list after all actions.

---

20. Mini Project - API User Manager

Build a small script with:

- base URL constant
- 4 CRUD functions
- error handling
- one demo flow that creates, updates, and deletes a user
