# Lesson 9 - Homework: Objects in JavaScript

Complete all 20 tasks at home. These are slightly harder than class tasks.

---

1. Create object `phone` with keys `brand`, `model`, `price`, and `color`.

---

2. Print the `brand` and `price` of `phone`.

---

3. Update the `price` of `phone`.

---

4. Add property `is5G` to `phone`.

---

5. Delete property `color` from `phone`.

---

6. Create variable `propertyName = "model"` and print the value using bracket notation.

---

7. Create object `teacher` with keys `name`, `subject`, and `experience`.

---

8. Add property `academy` to `teacher`.

---

9. Create nested object `teacher.contact` with keys `email` and `phone`.

---

10. Print `teacher.contact.email`.

---

11. Create object `movie` with keys `title`, `year`, `rating`, and `genre`.

---

12. Use `for...in` to print all keys and values in `movie`.

---

13. Count how many keys are in `movie` using `for...in` and print the total.

---

14. Create object `bankAccount` with keys `owner`, `balance`, and method `showBalance`.

---

15. Call `bankAccount.showBalance()`.

---

16. Create object `playlist` with keys `title`, `songs`, and `creator`. Make `songs` an array.

---

17. Print the first song from `playlist.songs`.

---

18. Add one more song to `playlist.songs`.

---

19. Create summary object `dashboard` with:
    - teacherName
    - movieTitle
    - totalSongs

---

20. Mini Project - Student Profile

Create one object named `studentProfile` with:

- basic info (`name`, `age`, `city`)
- nested object `contact`
- array `skills`
- method `introduce`

Print the object, print one nested value, print one array item, and call the method.
