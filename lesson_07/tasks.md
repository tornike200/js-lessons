# Lesson 7 - In-Class Tasks: Arrays Basics

Complete all 20 tasks in class. Use `console.log()` for outputs.

---

1. Create an array `cities` with 4 city names and print it.

---

2. Print the first city.

---

3. Print the last city using `length`.

---

4. Replace the second city with a new value.

---

5. Add one city to the end using `push()`.

---

6. Remove one city from the end using `pop()`.

---

7. Add one city to the beginning using `unshift()`.

---

8. Remove one city from the beginning using `shift()`.

---

9. Print the array length.

---

10. Create a copy of first 3 items using `slice()`.

---

11. Use `splice()` to remove 2 items from the middle.

---

12. Print both the removed items and updated original array.

---

13. Create an array `scores` with 5 numbers.

---

14. Sort `scores` in ascending order and print.

---

15. Reverse `scores` and print.

---

16. Create a string `"frontend"` and convert it to array of chars.

---

17. Reverse the char array.

---

18. Join chars back into a string and print.

---

19. Create an array `students` and print each name using a loop.

---

20. Build a mini output summary object:
    - total cities
    - first city
    - last city
      Print the object.
