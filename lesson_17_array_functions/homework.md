# Lesson 17 - Homework: Array Functions in JavaScript

Complete all tasks at home. These are slightly harder than class tasks.

---

1. Create an array of numbers and filter out all negative numbers.

---

2. Create an array of product objects with `name` and `price`. Use `map()` to get prices of all products.

---

3. Create an array of names and use `find()` to locate a specific person.

---

4. Create an array of numbers and use `reduce()` to calculate the product (multiply all numbers).

---

5. Create an array of temperatures and use `filter()` and `map()` to convert temperatures above 30°C to Fahrenheit.

---

6. Create an array of user objects and use `find()` to locate a user by ID.

---

7. Create an array of numbers and use `reduce()` to find the maximum value.

---

8. Create an array of transactions and use `reduce()` to calculate the total balance.

---

9. Create an array of objects and chain multiple array methods (filter -> map -> reduce).

---

10. Create a function that takes an array and returns a new array with only unique values using `filter()`.

---
