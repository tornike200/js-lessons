# Lesson 17 - In-Class Tasks: Array Functions in JavaScript

Complete all tasks in class. Use `console.log()` for outputs.

---

1. Create an array of numbers and use `filter()` to get only even numbers.

---

2. Create an array of words and use `map()` to convert them to uppercase.

---

3. Create an array of numbers and use `find()` to get the first number greater than 10.

---

4. Create an array of numbers and use `reduce()` to calculate the sum.

---

5. Create an array of objects with `name` and `age`, then use `filter()` to get people older than 18.

---

6. Create an array of numbers and use `forEach()` to print each number multiplied by 2.

---

7. Create an array of numbers and use `some()` to check if any number is greater than 50.

---

8. Create an array of numbers and use `every()` to check if all numbers are positive.

---

9. Create an array of numbers and use `includes()` to check if it contains the number 5.

---

10. Use `findIndex()` to find the position of a specific value in an array.

---
