# Lesson 6 - In-Class Tasks: DOM Manipulation Intro

Complete all 20 tasks in class. Use `console.log()` to print results where needed.

---

1. Select the `h2` element and print it in the console.

---

2. Select the element with id `title2` and print it.

---

3. Select the element with class `para1` and print its text.

---

4. Select all elements with class `para` and print the NodeList.

---

5. Print the text of the first element in `.para`.

---

6. Change the `h2` text to `DOM Manipulation Basics`.

---

7. Change `#title2` text to `JavaScript can update this`.

---

8. Set `h2` color to white and background color to dark green.

---

9. Add `padding: 12px` and `borderRadius: 8px` to `h2`.

---

10. Change `.para1` color to purple.

---

11. Change the color of all `.para` elements to orange using `forEach`.

---

12. Then change only the first `.para` element to red.

---

13. Create an array named `cities` with 3 city names and print it.

---

14. Print the last city from the `cities` array.

---

15. Select button `#btn1` and print it.

---

16. Add a click event on the button that prints `Clicked!`.

---

17. Create a boolean variable `isClicked` and set it to `false`.

---

18. On button click, if `isClicked` is false, set `h2` background to red and set `isClicked` to true.

---

19. Otherwise set `h2` background to yellow and set `isClicked` to false.

---

20. Add one more style change inside the click event (for example, text color or border).
