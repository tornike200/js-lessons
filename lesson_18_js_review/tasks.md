# Lesson 18 - In-Class Tasks: JavaScript Review

Complete all tasks in class. Use `console.log()` where needed.

---

1. Create two variables (`const` and `let`) and explain why each is used.

---

2. Check if a number is even or odd using `if/else`.

---

3. Use a `for` loop to print numbers from 1 to 10.

---

4. Create a function `getSquare(num)` that returns the square of a number.

---

5. Create an array of 5 numbers and print the last element.

---

6. Use `map()` to create a new array where each number is multiplied by 3.

---

7. Use `filter()` to get only numbers greater than 20.

---

8. Use `reduce()` to calculate total sum of array values.

---

9. Create an object `student` with `name`, `age`, `isActive` and print one value.

---

10. Add a method `intro()` to the object that returns `"Hi, I am NAME"`.

---

11. Select an element with id `title` and change its text.

---

12. Add a click event to `#reviewBtn` that toggles title color.

---

13. Create a small array of tasks and render them as `<li>` items inside `#taskList`.

---

14. Write an async function that fetches users from:
    `https://jsonplaceholder.typicode.com/users`
    and logs first 3 names.

---

15. Add `try/catch` to your fetch function.

---

16. Save a theme value (`light` or `dark`) into localStorage.

---

17. Read the saved theme from localStorage and print it.

---

18. Create a function that receives an array of prices and returns only prices below 50.

---

19. Chain `filter()` and `map()` on an array of numbers.

---

20. Mini integration: On button click, add one new task item and update a counter in DOM.

---
