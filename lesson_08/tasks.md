# Lesson 8 - In-Class Tasks: Loops in JavaScript

Complete all tasks in class. Use `console.log()` for all outputs.

---

1. Use a `for` loop to print numbers from 1 to 10.

---

2. Use a `for` loop to print numbers from 10 down to 1.

---

3. Create an array `cities` with 5 city names. Use a `for` loop to print each one.

---

4. Use a `for` loop to print each city with its index number.

---

5. Create an array `numbers = [3, 7, 12, 5, 18, 1, 9]`. Use a `for` loop to find and print the largest number.

---

6. Use a `for` loop to print only the even numbers from `numbers`.

---

7. Use a `for` loop to calculate the sum of all numbers in `numbers`. Print the total.

---

8. Use a `while` loop to print numbers from 0 to 7.

---

9. Use a `while` loop to go through the `cities` array and print each city.

---

10. Write a `do...while` loop that prints `"Hello!"` 4 times.

---

11. Create an array `words = ["hello", "world", "javascript", "code"]`. Use `for...of` to print each word.

---

12. Use `for...of` to print each character of the string `"Georgia"`.

---

13. Create an object `car = { brand: "Toyota", model: "Corolla", year: 2022 }`. Use `for...in` to print each key and its value.

---

14. Use a `for` loop with `continue` to print only odd numbers from 1 to 20.

---

15. Use a `for` loop with `break` to stop printing once you reach the number 5 in `numbers`.

---

16. Use a `for` loop to count how many numbers in `numbers` are greater than 5. Print the count.

---

17. Use a `while` loop to keep multiplying a variable `n = 1` by 2 until it exceeds 100. Print how many steps it took.

---

18. Create an array `scores = [45, 82, 60, 91, 38, 74]`. Use a `for` loop to print `"Pass"` for scores 60 and above, and `"Fail"` for below 60.

---

19. Use a `for` loop to build a new array `doubled` that contains each number from `numbers` multiplied by 2. Print `doubled`.

---

20. Use `for...of` to loop over `scores` and print the sum of all scores.

---

10. Use `findIndex` on `words` to get the index of the first word that starts with the letter `"c"`.

---

11. Create an array `prices = [120, 45, 300, 89, 250, 15, 60]`. Use `filter` to get prices under 100.

---

12. Use `map` on `prices` to add 10% to each price (multiply by 1.1).

---

13. Use `find` on `prices` to get the first price that is exactly divisible by 5.

---

14. Chain `filter` and `map`: from `prices`, keep only prices above 100, then multiply each by 2.

---

15. Create array `students = ["Ana", "Giorgi", "Mia", "Luka", "Nino"]`. Use `forEach` to print a greeting for each: `"Hello, Ana!"`.

---

16. Use `map` on `students` to create a new array of objects: `{ name: "Ana", greeting: "Hello, Ana!" }`.

---

17. Use `filter` on `students` to keep only names with 4 or fewer characters.

---

18. Use `findIndex` on `students` to find the index of `"Mia"`.

---

19. Chain `filter` and `forEach`: from `numbers`, keep only even numbers and print each one.

---

20. Use `map` to create a new array from `students` where each name has `" ✓"` added to the end.
